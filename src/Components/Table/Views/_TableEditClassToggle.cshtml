@using Htmx.Components.Table.Models
@using Htmx.Components.Table
@model ITableModel

@{
    var isEditing = Model.Rows.Any(r => r.IsEditing);
}

<div id="@TableComponentIdentity.EditToggleId(Model)" data-hc-table-edit-toggle class="@(isEditing ? "editing-mode" : "")"></div>
